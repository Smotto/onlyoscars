extends layout

block content
  .body
      .head
        h1#ootitle= title
      .welcome
        p Welcome to #{title}
          //#rawJSON-id This is a div

          //input#searchbar-id(type=search placeholder="Search for movie title" name=q )
          //ul#charactersList
          //button#search-button Search
          //#searchtext This is text that can be changed with the Search function
          //body This should be a #{body} of data.
      .wrapper
        .search-input
            input#searchbar-id(type='text' placeholder='Enter and Select Year')
            .autocomplete-box
                ul
            //button.button#yearSelectButton Select
      var#moviedataJSON #{JSON.stringify(moviedata)}
      .dropdowngroup
          select#category.form(name='default')
      var#category-name This should change when category is selected
      button#SearchButton Search Movies

      script(type = module src='/javascripts/frontend.js')

